{% extends '@Admin/index.html.twig' %}

{% block title %}Fiche Adhérent : {{ adherent.name }} - Administration Générale{% endblock %}

{% block main %}
    <section class="app-section pagetitle">
        <h1>Fiche Adhérent : {{ adherent.name }}</h1>
    </section>

    <section class="app-section alt">
        <div class="app-flex">
            <div class="flex-item actions">
                <a href="{{ path('admin_adherents_index') }}"
                   class="primary"
                   title="Retour à la liste">
                    <i class="fa-solid fa-hand-point-left"></i>
                </a>
            </div>

            <div class="flex-item">
                <strong>Type</strong>
                {{ adherent.adherentType }}
            </div>

            <div class="flex-item">
                <strong>E-mail</strong>
                {{ adherent.email }}
            </div>

            <div class="flex-item">
                <strong>Téléphone</strong>
                {{ adherent.phone ?? '&nbsp;' }}
            </div>

            <div class="flex-item">
                <strong>Adhérent depuis</strong>
                {{ adherent.createdAt | date('d/m/Y à H:i') }}
            </div>

            <div class="flex-item actions">
                <a href="{{ path('admin_adherents_edit', {'id': adherent.id}) }}"
                   class="info"
                   title="Modifier la fiche adhérent">
                    <i class="fa-solid fa-pen-to-square"></i>
                </a>

                <a href="{{ path('admin_archives_adherents_archive', {'id': adherent.id}) }}"
                   class="warning"
                   data-controller="popup--index"
                   data-action="popup--index#open"
                   data-popupid="popup-archive-{{ adherent.id }}"
                   title="Archiver l'adhérent">
                    <i class="fa-solid fa-box-archive"></i>
                </a>
                <div id="popup-archive-{{ adherent.id }}" class="app-popup">
                    <div class="popup-body">
                        <div class="popup-header">
                            <h2>Archiver un Adhérent</h2>
                            <a href="#"
                               class="close"
                               data-controller="popup--index"
                               data-action="popup--index#close"
                               data-popupid="popup-archive-{{ adherent.id }}">
                                <i class="fa-solid fa-circle-xmark"></i>
                            </a>
                        </div>
                        <div class="popup-content">
                            <h3>
                                {{ adherent.name }}
                                -
                                {{ adherent.adherentType }}
                            </h3>

                            <form action="{{ path('admin_archives_adherents_archive', {'id': adherent.id}) }}"
                                  method="post">
                                <label for="archivedMotivation" class="form-label required">Motif :</label>
                                <input type="text"
                                       name="archivedMotivation"
                                       id="archivedMotivation"
                                       class="form-control"
                                       required/>
                                <button type="submit" class="app-btn cta">Archiver</button>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
{% endblock %}
